import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.relevamiento.parcial',
  appName: 'Relevamiento Visual',
  webDir: 'www'
};

export default config;
